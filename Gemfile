# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Pin bigdecimal to avoid native extension build issues
gem "bigdecimal", "~> 3.1"

group :development do
  gem "rake", "~> 13.0"
end
